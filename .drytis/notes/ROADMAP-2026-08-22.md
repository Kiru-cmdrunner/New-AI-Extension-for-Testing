# CmdRunner Master Roadmap — toward the Application Understanding Engine
(2026-08-22, post 6A+6C validation. Anchored at HEAD 568adfe + validated 6A+6C tree.)

VISION: CmdRunner is an Application Understanding Engine. AdaniOne (and the clone
harness family) are validation benchmarks only. Priorities: (1) accurate semantic
capture, (2) Application Knowledge Repository quality, (3) capability model
creation, (4) future execution / codegen / AI agents & assistants.
DOCTRINE (immutable): deterministic-first, AI-last; no timing rules; S3' no
fabricated completions; honesty over fabrication (skip ≠ loss); generation never
imports understanding (INV-GEN-8/10); no keyword-dictionary / System-B revival;
user is the live-site evidence loop (Akamai-blocked egress); commit only on owner
approval; evidence-gated changes only.

## NOW — 6A+6C stabilization (in flight)
1. Fix E9/rung-7: identity-coordinate gate in classification (unlabeled
   non-numeric changes SKIP) — honesty violation, KR-polluting.
2. Fix mixed-numeral demotion ("5 items" → counter, not notification).
3. Add missing pins: ⑧ display (renderResultingState), ⑨ KR round-trip
   (wireToObservedItem), E9, anchor-less ambiguity, O6-STOP.
4. Reconcile duplicated stamp helper; split consequence-settling.test.ts out.
5. Re-run: full suite + multipattern + clone + int47 + ZIP + Amazon + Pax.
6. Owner gate → commit 6A+6C (second engine-loop commit).
   Harness hygiene alongside: update stale bug-witness check in multipattern
   (it demands the pre-6C broken shape).

## PHASE 6 (continuing) — Real-Site Derivation Quality
- 6B Locator durability (P1 capture): ElementIdentity gains data-auto-id +
  className candidates; locator-ranking gains BUSINESS tier. Unlocks: replay
  durability, icon-step locators (O9), stronger KR element signatures. Gates:
  identity vocabulary pins, clone+matrix, no ranking regressions.
  **SHIPPED 2026-08-22** — full closure: 250 files/4405 tests green, tsc 8
  pre-existing only, real-Chrome E2E 7/7 (archived: .drytis/evidence/phase-6b-e2e-2026-08-22/),
  reviewer + infra_verifier PASS, owner gate. data-testid stays bare (STAB);
  non-default families family-tagged in the VALUE; healing set-diff; harvest
  key chain unchanged (dataAutoId appended last). Commit: 6B src+tests, then
  docs/spec/evidence. L4 remainder (notifications + status-badge family
  variants) → **owned by 6D.1** (per AC5 fold decision 2026-08-22; closed by
  6D.1 W1, 2026-08-23).
- 6D.1 Universal interaction classes — **SHIPPED 2026-08-23** (W1–W4):
  W1 notification ([aria-live], [role="log"], snackbar, toast-body/content)
  + status-badge ([data-state], [aria-pressed], [aria-current],
  [aria-selected="true"]) selector families — additive-only; W2 #id-only
  counter seeding via 1/1 text-swap shape gate (closes full-audit O8 miss:
  `p#result-count` "2 tickets"); W3 options-list/suggestion/autocomplete/
  typeahead tokens → click claims as plain Click (closes audit options-list
  miss); W4 DatePicker placeholder-only trigger (shared DATE_NAME_HINT_RE).
  Full closure: 254 files/4464 tests green, tsc exactly the 8 pre-existing
  baseline errors, real-Chrome E2E **16/16 PASS** with explicit 6A/6B
  regression proof runs (archived:
  .drytis/notes/evidence/phase-6d1-e2e-2026-08-23/), reviewer + infra PASS,
  owner gate. §2.3 five decisions held: no standalone [class*="toast"];
  1/1 text-swap gate only; Click classification (no new InteractionType);
  dataAutoId fuzzy + deriveAppId parked; **no Toggle/filter-chip definition
  (deferred → 6D.2+ with P1 census)**. Known transient (pre-existing, not a
  regression): badge snapshot may read transient pending text (e.g.
  "Planning…") — byte-identical to 92de517 baseline dumps. Carried forward:
  filter-chip/Toggle definition + flight-card entity recognition via 6E-slice
  clone (aria-label cells / no-data-count / widget-named data-auto-id) →
  6D.2/6E; P1/P2 census consumption → 6D.2. dataAutoId fuzzy-matching
  consistency (alias family resolution parity across capture/executor/
  renderer) remains **future 6D/7.1 backlog** — tracked here, deliberately
  not in 6B/6D.1.
- 6D.2 semantic-surface precision — **CLOSED 2026-08-23, evidence-resolved at
  M1, ZERO implementation shipped** (owner closure 05:34 UTC). The roadmap
  entry's original clauses were stale (grounding audit + M1 replaced them with
  evidence): (1) "ARIA dropdown remainder" was already solved by 6D.0/6D.1
  (selectionConfirmed since 68d831e; W3/W4 tokens shipped; E2E-verified).
  (2) **O11 unpaired-mousedown pairing: canonical orphan NOT-REPRODUCED** —
  resolveTarget promotes nameless children to the named ancestor, so both
  press halves resolve identically and S1' pairs them (real-Chrome 14 PASS/0
  FAIL over 5 press shapes). M2 descoped. Only residual: cross-target drag
  (down on A, up on B, click on common ancestor → 2 honest unpaired cards)
  → **6F display backlog** (is two cards right for a drag?; IR unaffected).
  (3) **Popup-token anchoring: CLOSE-AS-IS** — extended 32-case P3 re-probe
  proves anchoring irreducible: every anchored variant retaining `popup`
  over-matches popup-banner/footer/cookie shapes; dropping popup misses
  traveler-popup positives; anchored-without-popup would BREAK 6D.1 W3
  positives (suggestion-list/options-list). Shipped regex has 0 misses;
  over-match bounded+honest (worst case noisier card). Recorded; revisit
  only as a 6F display-naming knob, never gating. (4) **Toggle/filter-chip:
  census says population 0** — aria-pressed/role=switch/data-state uncovered
  interactive elements: clone 0/9, 6D.1 app 0/5, Avis Ford LIVE 0/249,
  Amazon G5-archived 0 recorded (role=checkbox/menuitemcheckbox/details 0;
  aria-pressed unmeasured live — container blocked; user evidence loop if
  ever needed). **M4 NO-GO: Toggle stays parked.** Phase spends no code.
  Evidence: .drytis/notes/evidence/phase-6d2-m1-2026-08-23/ + spec
  .drytis/specs/phase-6d2-semantic-surface-precision.md (§3.1). Carried
  forward: flight-card entity recognition → 6E-slice (unchanged owner);
  P1/P2 census consumption → folded into M1-B (done).
- 6E full: clone fidelity as standing process — clone mirrors real-site
  attribute reality from every user evidence batch.
- 6F Display hygiene: O1 naming override map, O2 no-op row suppression, O7
  duplicate surfaces, O13 placeholder sweep at STOP, O14 nav naming, O12 RC7
  orphan display. Panel tells the truth beautifully. **From 6D.2 M1 (2026-08-23):**
  cross-target drag twin display (2 unpaired cards: mousedown@A + synthesized
  click@common-ancestor — decide collapse/naming for drag-shaped gestures;
  IR unaffected; see phase-6d2-m1 evidence).
- 6F-eng (engine backlog, from MS-U2 validation 2026-08-22): settle-mode
  delivery discards AdaptiveWindow's stabilityTrace for every
  consequence-settled window (evidence-collector.ts:1841 hardcodes
  stabilityTrace: []; samples exist — adaptive-window.ts:356-368 records
  them; EvidenceWindow carries them at :191). Symptom: side panel D6
  window-internals drill-down never renders for the most evidence-rich
  window class; knowledge consumers get an empty trace. Fix: copy
  evidenceWindow.stabilityTrace at :1841 (the window closeWindow already
  receives), pin samples>0 for a consequence-settled window in the unit
  layer. See notes/evidence/sidepanel-e2e-2026-08-22/MSU2-VALIDATION.md.
- USER EVIDENCE LOOPS (continuous): batch-4 manual run post 6A+6C commit →
  assertion-rich form cards (§8 expectation); post-6B → locator durability;
  post-6D.1 → results-page recognition (O10 closes). Amazon F1–F4 findings
  fold in: F1 targetCss loss → 6B (shipped); F2 #nav-cart-count → 6A on
  Amazon reality; F3 autocomplete-popup-as-collection → CLOSED 6D.2 M1-C
  (anchoring irreducible, over-match bounded; see phase-6d2-m1); F4 panel
  arming + active-tab binding → service-worker lifecycle fix in 6F window.
- 6E-M2 (2026-08-23, SHIPPED — commit 1 `24e1340` src+tests, commit 2
  spec+evidence+roadmap, owner gate 10:02 UTC, unpushed): react-datepicker
  family —
  separator-tolerant trigger token, travel-date name vocab, cell class
  `datepicker__day` + W3C name-shape cells, cell-never-triggers honored on
  the ANCESTOR path (nested-wrapper double-spawn fixed in round 2). User
  batch-1 'dates Unclassified' defect CLOSED. Residuals for 6F resolved by
  6F-M1 (2026-08-23, SHIPPED — commit 1 src+tests `89ff297`, commit 2
  spec+evidence+roadmap `(this commit)`, owner gate 12:51 UTC, unpushed):
  paired cell click absorbed by the structural gesture-ownership rule
  (same pageId + elementKey, exact discrete-event adjacency — the falsified
  `captureSeq+1` draft corrected mid-implementation from E2E run-1; no
  timing fields, doctrine-pinned by source test) — twin Click card +
  duplicate IR step gone (7 cards/7 steps → 5/5); KR parameter VALUE
  mapping fixed (textValue ?? dateValue ?? valueAfter — params non-null,
  E2E-verified in Dexie dump); settle-mode stabilityTrace delivery restored
  (6F-eng backlog item CLOSED). Remaining 6F backlog: F4 panel arming +
  active-tab binding → SW lifecycle fix; display hygiene batch (O1/O2/O7/
  O12/O13/O14 + cross-target drag twin); WARN-4 (a click consumed by an
  ACTIVE lifecycle at runtime step 3 does not supersede gesture records —
  bounded, logged in phase-6f-m1 spec changelog).
- 6F-M2b (2026-08-23, SHIPPED — commit 1 src+tests `2bff6ed`, commit 2
  spec+evidence+roadmap `(this commit)`, owner gate 16:54 UTC, unpushed):
  F4-D CLOSED — sw-recovered-form-submit cards now seed
  targetEvidence.identity from the owning interaction's trigger (guarded
  clone, honesty over fabrication: shape-less triggers keep null, before/
  after stay null). The "Unknown element" symptom on recovered click cards
  is gone (user batches 2026-08-23-0700/0724 — the card mid-navigation
  renderer-loss case now shows the real element). E2E required a harness
  design iteration (15 logged runs): the recovery window between click-card
  commit and the pagehide flush is ~40 ms, so the harness uses a
  conjunction trigger (poll storage for durable stamp + bare click card,
  then Page.crash) under 6× CPU throttle — deterministic hit, 9 PASS/0
  FAIL; M1 regression 9 PASS/0 FAIL on the same build. Consumer sweep
  pinned: only renderIdentity + TargetStateSignalExtractor read
  targetEvidence.identity; richness scoring unchanged. F4-L (panel arming /
  active-tab binding SW lifecycle) remains PARKED-NO-REPRO: grounding audit
  found manifest-declared <all_urls> injection, 3-scenario
  ensureContentScriptInjected re-sync, debounced health checks — no live
  arming repro on v10.9.0
  through any post-6D.1 E2E; re-open only against a current reproducible
  failure (doctrine: evidence-resolved, same as O11 in 6D.2). Remaining 6F
  backlog: display hygiene batch (O1/O2/O7/O12/O13/O14 + cross-target drag
  twin); WARN-4 (bounded, logged in phase-6f-m1 spec changelog).
- 6F-M3 Wave 1 — panel display honesty pass **SHIPPED 2026-08-24**
  (O13/O2/O12/O14 only; spec
  .drytis/specs/phase-6f-m3-w1-display-honesty.md). O13 CLOSED: stopped view
  renders an honest terminal note ("No behavioral evidence captured…") —
  "Collecting…" never appears after STOP (E2E-verified). O2 CLOSED: no-op
  DOM rows (attr-only old==new) suppressed with "· N no-op hidden" header;
  missing delta counts as material (honesty guard); raw evidence untouched.
  O12 CLOSED (display half): no-owner sw-recovered windows explain
  "Unknown element" via one synthetic-notice line; silent when the 6F-M2b
  identity seed is present. O14 CLOSED: displayUrl (origin+path, query/hash
  dropped, 60-char truncation) at both label sites; IR/KR keep the full raw
  URL (AC6 E2E-verified). TWO GENUINE E2E FINDINGS folded back: Chrome
  URL-synthesizes tab titles for untitled pages (full query URL — and its
  scheme-stripped form — as pageTitle) → isUrlDerivedTitle fallback added
  at both sites; harness assertions scoped to card labels vs IR raw-URL
  retention. Gates: suite 267/4,562 green, tsc 8-baseline, real-Chrome E2E
  11 PASS / 0 FAIL (runs 1–9 logged, two findings iterated), M1 DatePicker
  regression 9/0, reviewer PASS (WARNs closed), infra_verifier PASS, ZIP
  md5 e31d48e8 live-verified. Remaining 6F backlog: WARN-4 micro-spec
  (engine, own test matrix); O7 parked-no-repro (0 alert-surface entries
  across all committed dumps); cross-target drag twin (owner decision);
  O1 naming vocab (standing process); S5 re-drain (deferred, ≈0 today).

## PHASE 7 — Knowledge Repository maturity (vision priority 2+3)
- 7.4-B1 'Expander definition' (2026-08-25, SHIPPED — commit 1 `5140736`
  src+tests+fixture, commit 2 spec+evidence+roadmap+handover; see
  `.drytis/specs/phase-7-4-b1-expander.md` + evidence PROVENANCE at
  `.drytis/notes/evidence/phase-7-4-b1-e2e-2026-08-25/`): first 7.4-B
  'Semantic Control Understanding' slice — `aria-expanded` (W3C disclosure
  convention) now yields a typed `Expander` interaction instead of Click/
  Unclassified. One definition @ priority 80 (below Tab 65/Dropdown 20 which
  keep their types when also aria-expanded; above Click 180 fallback) + 6
  wiring touchpoints (union, registry, IR map, badge map, DDC-3 allowlist,
  panel TYPE_DISPLAY). Replay-safe: Expander → IRAction.CLICK deliberately —
  TOGGLE is checkbox-specific (sets .checked) and would no-op on div/button.
  Three genuine defects surfaced during the cycle, all fixed: (1) strict
  `!== null` claim gate over-claimed `undefined` in legacy DomContexts —
  caught by the OLD 6D.1 W3 runtime pin, now boolean-valued + B1-12 pin;
  (2) self-inflicted registry corruption (comment edit joined two array
  lines in index.ts, dropping navigationDefinition → 13 nav/attribution
  failures — the full-suite gate caught it; registry array is load-bearing);
  (3) two harness assertion bugs (wrong storage paths) — engine was correct
  all along. Gates: 21 new tests (suite 286/4,715), tsc 8, ZIP md5
  51b83f9d four-way identical, reviewer PASS (4 WARNs addressed: B1-13
  no-steal pin, B1-17 real signature-divergence pin, X4c panel-badge E2E
  after post-STOP panel reload, stale comments), infra PASS, E2E 16/0
  (button/role=button/bare-div shapes, flips ×3, state rows ×3, IR zero
  toggles, contracts expanded ×3, panel renders 3 🔽 badges), house
  regressions 6E-M2 8/0 + 6F-M1 9/0 + 7.4-M1 16/0. Accepted costs (pinned):
  KR signature fragmentation for controls previously typed Click (cold
  restart, demote-not-destroy); 2s dedup merges rapid expand→collapse pairs;
  child-held aria-expanded stays invisible (resolution limit). NEXT: 7.4-B2
  candidates — Modal (no definition; modal-tracker.ts dead code) or
  Autocomplete (combobox typing swallowed by Dropdown), owner to pick.
- 7.4-B2 'Typeable combobox honesty' (2026-08-25, SHIPPED — commit 1
  `393ad61` src+tests+fixture, commit 2 docs; see
  `.drytis/specs/phase-7-4-b2-combobox-typeable.md`): owner picked
  Autocomplete; grounded audit found the true defect family: (F1) typeable
  role=combobox INPUTs claimed by Dropdown at priority 20 → typed query
  invisible (no TextEntry card, typedValue lost, keyboard-commit path had
  NO completion at all); (F4) IR bridge mapped INPUT-triggered Dropdown →
  executeSelect on .options — non-replayable on div-option comboboxes;
  (dead-path) Dropdown buildResult never wrote selectionConfirmed=true →
  panel why-line variants unproducible (note
  dropdown-whyline-dead-path.md, RESOLVED). Fix per approved spec: S1
  substrate (ariaAutoComplete + listId on DomContext/extractor, M1
  conditional pattern), S2a shape (a) honest decomposition — Dropdown
  disclaims typeable inputs (isTextEntry && readOnly!==true), W-A.3 option
  guard, DatePicker disclaims aria-haspopup=listbox (narrow — dialog still
  claims, MUI safe), TextEntry gains comboboxSignal metadata + panel
  why-line; S2b IR honesty — INPUT-triggered confirmed Dropdown emits two
  CLICK steps (trigger + option) via findCompletingOptionEvent; tag=SELECT
  keeps single SELECT. Readonly OXD path unchanged (R1 E2E-pinned).
  Evidence: 3-run E2E history — run 1 against stale dist captured the
  PRE-fix production shape (select step + missing selectionConfirmed) =
  empirical proof the defect was live; run 3 after rebuild: IR
  [fill,click,fill,fill,click,click,click,fill,click], 18/18, 0 console
  errors. Gates: suite 292/4,764 (+6 files/+49 tests), tsc 8, ZIP md5
  762de56e two-way identical (dist/serve manifest four-way c204e8d2),
  reviewer PASS (4 of 8 WARNs fixed: 9b tautology pin, raw Unclassified
  assertion, PROVENANCE, whyline note RESOLVED; 4 doc-only deferred), infra
  PASS. Mid-cycle lesson: ALWAYS rebuild dist before E2E — the harness
  loads dist, not src. Owner-directed read-only audit (2026-08-25) of the
  'capture-every-click / exclusion-only' alternative model concluded:
  architecture is ALREADY capture-first (tap→ledger→projection preserves
  every discrete event as Click or Unclassified); reversal would fabricate
  actionhood, degrade replay, destroy the KR cross-session learning
  signal. Two genuine silent-loss channels found (accumulating input/change
  never ledgered → typed text with no completing lifecycle is lost;
  structural BODY/HTML/SVG targets return null → click-away dismissals
  never enter the ledger). NEXT: 7.4-B3 'capture completeness residuals'
  proposed (census + loss channels + adapter/bridge policy reconciliation
  where toIRAction maps Unclassified→CLICK but ir-bridge drops them);
  owner to pick vs Modal.
- 7.4-B3 'capture completeness residuals' (2026-08-25, SHIPPED — commit 1
  `96f0eb9` src+tests+fixture+analyzer, commit 2 `21e6bf8`
  spec+evidence+docs; see `.drytis/specs/phase-7-4-b3-capture-completeness.md`):
  closed the two silent-loss channels the audit found, without reversing
  the qualification-first model. S0 census
  (`scripts/unclassified-census.mjs` + `public/census-validation.html`)
  established the first baseline taxonomy of Unclassified residuals
  (dedup-resurrected / gate-rejected / evidence-consequential /
  body-structural). S1 STOP-time evidence drain onto projected
  Unclassified cards (eventId join, persist-exactly-once guard). S2 dedup
  fold — a suppressed duplicate now folds into its prior lifecycle
  (members appended eventId-guarded, ledger 'claimed' by prior,
  repeatCount, twin mousedown claimed) instead of resurrecting as a
  half-card; degrades to legacy suppress+release post-restart (accepted,
  pinned). S3 typed-text terminal sample — ONE synthetic ledger 'change'
  entry per typing episode (blur closes; counter-start 1e9 keeps
  pageId-parsing + foldIntoPrior twin scan intact; sampled at LAST INPUT
  not blur, so it never splits a BODY gesture). S4 BODY/HTML removed
  from NON_INTERACTIVE_TAGS (HEAD/SCRIPT/STYLE/SVG-internals stay) —
  click-away dismissal now enters the ledger. S5 actionabilityEvidence
  presentation flag (consequence-window match; no IR/KR change, R8).
  C3 RCA: dedup-folded lifecycles never emit → FINALIZE_EVIDENCE never
  fired → stale EvidenceCollector bindings held later windows open past
  the S1 drain; fixed by finalizing the SUPPRESSED lifecycle at fold
  time. Evidence: definitive run b3-full-final3 14 PASS / 0 FAIL, 16
  cards, census unclassifiedTotal 14 = 10 gate-rejected + 1
  body-structural + 3 evidence-consequential, 0 dedup-resurrected,
  zero select in IR; closure ZIP rebuilt after the download-server
  jailing fix (md5 `b702ac44…`). D1 (Unclassified relabel) resolved
  NO — signatureKey = appId|actionType|normalizedTarget|anchorViewId is
  KR knowledge identity; a relabel is a vocabulary-freeze migration.
  D2 (adapter EMIT vs bridge DROP) deferred to a dedicated audit.
  NEXT: D1/D2 grounding audit, then 7.4-B4 vs Modal.
- 7.4-B4 'Unclassified output policy reconciliation (D2 DROP)' (2026-08-26,
  SHIPPED — commit 1 `1edbf41` src+tests, commit 2 docs; see
  `.drytis/specs/phase-7-4-b4-unclassified-output-policy.md`; duplicate
  draft `phase-7-4-b4-ir-honesty-consolidation.md` marked SUPERSEDED —
  its enum-import plan would have emitted lowercase literals breaking 5
  live test files): D2 grounding audit found TWO converters — live
  `ir-bridge.ts build()` (sole ExecutionIRPlan producer, NOISE_TYPES =
  Scroll+Unclassified → DROP) vs DEAD `output-adapter.ts toIRAction`
  (EMIT CLICK/RIGHT_CLICK; tree-shaken from the shipped bundle — SW
  imports only filterProductionInteractions). B4 aligned the dead path
  to the decided policy: toIRAction returns null for ALL Unclassified
  physical types, physical-type switch deleted, local IRAction union
  reduced (RIGHT_CLICK removed — never in the domain enum, would throw
  in the Playwright renderer), repo-wide zero RIGHT_CLICK references.
  Tests: capture-guarantee-v2 GROUP 4 re-pinned EMIT→DROP (filter test
  preserved), adapter null-pins added, NEW bridge-drop coverage (black-
  box build(): zero steps carry any Unclassified sourceEventId), drain-
  time richness contest, cross-element blur arms, tap-layer body-blur
  boundary. X3 truth: semantic SW-bundle diff = exactly ONE comment
  line (PRE/POST archived); adapter tree-shaken. X4: real-Chrome E2E
  14/0 with census-flow IR plan IDENTICAL to B3 closure after volatile-
  id normalization; zero select steps (B2 parity held). Closure ZIP
  md5 `fabec454…` four-way identical. Result: exactly ONE Unclassified
  output policy (DROP) across adapter, bridge, both test surfaces, and
  ENGINEERING-HANDOVER §Layer 6 (stale RIGHT_CLICK/'10 types' fixed).
  D1 remains closed (KR identity constraint). S5 written disposition:
  the 9-per-key keydown residual is NOT grouped in B4 (revisit only on
  owner-visible complaint). NEXT: 7.4-B5 Modal definition.
- 7.4-B5 'Modal definition — dialog open + Escape dismissal' (2026-08-26,
  SHIPPED — commit 1 `544059c` src+tests+fixture, commit 2 spec+evidence+
  roadmap+handover; see `.drytis/specs/phase-7-4-b5-modal.md`): the last
  major IDENTIFY rung of the owner's 13-pattern matrix. Single Modal
  definition @ priority 75 (Tab 65 < Link 70 < Modal 75 < Expander 80),
  two trigger branches: open (click + strict aria-haspopup==="dialog") and
  dismiss (keydown + bare Escape + dialog in ancestry-or-self). Immediate
  completion (Expander shape). IR: open→CLICK element, dismiss→
  KEYBOARD_SHORTCUT with NoTarget {kind:"none"} + input="Escape". B5-2c
  implemented four replay touchpoints (renderer, both executors, ir-bridge
  NoTarget path), incidentally fixing F3 (latent modifier-shortcut replay
  defect — any recorded Ctrl/Cmd combo now renders + executes instead of
  throwing Unsupported IRAction). B5-2b no code change (in-overlay backdrop
  stays Click, sibling stays Unclassified — B3 parity). B5-4 deleted
  modal-tracker.ts (114 lines dead code). Capture-model audit verdict: the
  architecture is already capture-first at EventTap/ledger layers; the
  qualification question only affects the default label and burden of proof.
  E2E: 12 PASS / 0 FAIL (b5-full-final). F4 truth: late-dialog surfaces NOT
  captured (honest finding, deferred to future surface-capture arc). D1
    remains closed (KR identity constraint). NEXT: owner to pick — publish
  branch (10 commits ahead), then 7.4 C→D capability layer on a finished
  vocabulary.
- 7.4-B6 'Enter-submit commit — TextEntry completion grounded in
  application behavior' (2026-08-26, SHIPPED — spec
  `.drytis/specs/phase-7-4-b6-enter-submit-commit.md`; full verification
  record in spec §8): fixes the owner-confirmed field bug "type a product
  name, press Enter → only the Go/Search click + navigation captured; the
  typed value vanished". Root cause (real-Chrome CDP forensics): implicit
  form submission fires a TRUSTED synthetic click on the default submit
  button BEFORE the native submit event and with NO prior blur — the
  synthetic click hit TextEntry.shouldCancelOnOutside (different element)
  → lifecycle abandoned → filterProductionInteractions destroyed the value
  that lived only in ctx.data. Fix honors the owner's doctrine constraint:
  Enter keydown is NEVER a completion trigger (cause, not commit). The
  commit proof is the trusted native `submit` DOM event joined to the
  input's owner form by a new pure formJoinKey (elementKey of closest
  ancestor <form>, captured at event time into 5 new DomContext fields:
  formElementKey/formAction/formMethod/formId/isFormSubmitControl — the
  last is HTML ground truth incl. button-without-type = submit). Three
  changes: (1) TextEntry.isInScope accepts same-form submit events →
  handleEvent completes with commitSignal='submit' + committedValue +
  enterCause (display-grade flag; never used for classification/
  generation) — component-runtime.ts UNMODIFIED (delivery rides the
  existing offer loop); (2) shouldCancelOnOutside exempts same-form
  clicks (the commit cause) — different-form clicks still cancel (regres-
  sion-pinned); (3) ir-bridge restoreSubmitFillOrder moves the committed
  FILL before the earlier same-form submit-control CLICK (the TextEntry
  completes AFTER the click, so emission order would produce an
  unreplayable click-then-fill). Back-compat: null joins never match —
  legacy no-form events behave byte-identically (AC10 pin). Verification:
  22 B6 unit tests (incl. 6-test jsdom extractor pin), full suite 313
  files / 4889 tests green, tsc at the 8-error pre-existing baseline,
  reviewer PASS ×2 rounds (all WARNs remediated), tester PASS (fixture
  journeys on preview), infra_verifier PASS. Real-Chrome harness
  `.drytis/zz-enter-submit-verify.mjs` is now SELF-ASSERTING and
  idempotent (verdict PASS: fill→click→navigate, committedValue
  "wireless earbuds", 17/17 keydowns claimed, 0 SW errors). Known
  remaining gap (accepted, out of scope per spec §3): SPA JS submits
  (fetch/XHR) still complete only via blur/STOP-interrupt — same
  uncommitted-value loss shape exists there; flagged for a future B-slice.
  → CLOSED by 7.4-B6.1 below.
- 7.4-B7 'Hover evidence observation' P1 (2026-08-28, SHIPPED (P1 only —
  B7-P2/P3/P4 pending approval; spec `.drytis/specs/phase-7-4-b7-hover-evidence-observation.md`,
  audit-amended R-1..R-5 + W-1/W-3/W-5/W-8): evidence enablement, purely
  additive, zero semantic change. What P1 delivers: gated discovery enters
  (isHoverDiscoveryEnter via shared isInteractiveElement) open PROVISIONAL
  evidence windows holdOpen-from-birth (R-2 binding race; closed only by
  binding+terminal / STOP / pagehide / displacement / TRIGGER_REMOVED),
  AdaptiveWindow maxDuration suspended for hover windows (R-5; settle
  re-arms a fresh budget from settle entry), hover displacement preference
  at MAX_CONCURRENT_WINDOWS, TRIGGER_REMOVED producer via structural
  isConnected check riding every mutation batch (new endReason
  'element-removed'), R-3 dialog-drain membership for hover windows,
  definition-local member policy (W-6: pop mousemove members; enters stay
  as pointer-path facts capped 20 drop-oldest with counted
  pointerPathDropped), lifecycle endState threading, and TWO transitional
  containment gates (assertion-derivation + signal-extractor Hover skip)
  keeping IR/understanding outputs byte-identical to pre-P1 until P2/P4.
  Deleted: nothing yet (confidence model/vocab/meaningful stay until P2 —
  atomic swap). Verification: 6 P1 suites 26 tests green; full suite 324
  files / 4,954 tests green; tsc at 8-error pre-existing baseline;
  real-Chrome zz-b7-p1-validate.mjs 10/10 PASS (rich evidence on the
  meaningful hover, zero IR assertions, gated-not-in-production, F-2 F-2
  intact, no orphaned pending evidence); B6 + B6.1 harnesses re-verified
  PASS post-P1; baseline-vs-post canonical diff shows only the three
  intended evidence deltas (+domChanges, +pointerPathDropped count, real
  sourceEventType 'mouseenter'); infra_verifier PASS (file-server mirror
  refreshed after 09:06 rebuild); reviewer PASS (6/6 criteria, all prior
  WARNs remediated: producer, counted drops, hover-scoped settle remap);
  tester PASS (preview deliverable 4/4). Not published/deployed (owner
  gate). NEXT: B7-P2 definition/terminals/stamps/ledger/admission (atomic).
- 7.4-B7-P4 'Presentation, IR assertions, full gate' (2026-08-29, SHIPPED):
  panel per-class evidence chips (deriveConsequenceClasses rendered as
  `interaction-chip--consequence` chips; gesture-only renders none);
  gesture-only grouping via the EXISTING MS-U1 show-hidden toggle-row
  (grouping never destruction — D4; suppressed reason 'not meaningful'
  derives at render time); unattributedConsequences already surfaced via
  coverage rows (understanding-card, V1 honest floor); W-5 — Hover dedup
  EXEMPTED at capture (component-runtime isDuplicate early-return; folding
  destroyed lifecycle/evidence — F-5 data loss scoped away; presentation-
  only grouping replaces it); compat bridge DELETED (filterProduction-
  Interactions is a pure filter again; ledger isAnchorEligibleInteraction
  + panel isProductionInteraction/suppressionReason derive admission from
  recorded evidence via deriveConsequenceClasses at read time; stored
  metadata.consequenceClasses/meaningful are inert history); `surface-
  visible` joins DERIVABLE_KINDS + WireObservedItem kind union; P1 Hover
  containment gate REMOVED in deriveStepAssertions and replaced with the
  OWNERSHIP-SAFE derivation: ONE shared adversarial ownership pass
  (src/shared/surface-fact-ownership.ts — rule (a) batch boundary vs
  delivered window.openedBatch, rule (b) primary-action veto (click/
  contextmenu anchor T0 in [open, open+duration) with fact batch ≥
  click's openedBatch), rule (c) container precedence) computed once and
  consumed at TWO seams — causal-graph provenance links (semantics
  unchanged for hover producers; click episodes enter as claimants for
  the first time) and deriveForOwnedSurfaceFacts (locator-honest #id
  only, INV-GEN-4; MAX_ASSERTIONS_PER_STEP=3; mutation facts never
  derivable); Channel A V1+V2 must yield NO hover surface-visible
  assertion (real-Chrome validation gate); NOISE_TYPES/OR-1 untouched.

- 7.4-B7-P2 'Hover semantic swap — atomic' (2026-08-28, SHIPPED): the

  §5.2 atomic swap plus the three follow-on discoveries it forced.
  (a) THE SWAP: hover.ts rewritten observe-only — deleted CONFIDENCE_
  THRESHOLD, HOVER_TRANSIT_THRESHOLD_MS, SUSTAINED_DWELL_MS, POINTER_
  STATIONARY_RADIUS_PX, the confidence accumulator, evidenceReason,
  overlay-role/CSS/nav-ancestor regexes, stored metadata.meaningful,
  and shouldCancelOnOutside; six structural terminals (left,
  consumed-by-click, navigation, target-removed, recording-end,
  idle-timeout→abandoned); completesAtRecordingEnd declaration; gated
  discovery enters stamp SECONDARY create-only at the SW dispatcher
  (R-4 site gate); LEDGER_APPENDABLE/ANCHOR_ELIGIBLE decomposed (mousedown
  kept in ANCHOR_ELIGIBLE — W-3 Dropdown); gated enters claim ledger
  lives (W-8: five runtime disposition sites extended, pending-never-
  mints projection rule R-1, abandoned still mints the twin backstop);
  flushOnNavigation winner election generalized (B6/B6.1 preserved);
  admission rule = completed ∧ deriveConsequenceClasses>0 (reveal/
  insertion/removal/stamped-fetch/nav/revert/pointer-reach) with compat
  bridge writing consequenceClasses + derived meaningful until P4.
  (b) STOP pre-pipeline hover evidence drain: STOP_EVIDENCE_DRAIN SW→CS
  before the stop pipeline — drainHoverWindowsAtStop force-closes open
  provisional hover windows ('recording-stopped') so no-leave hovers
  (S5) deliver evidence BEFORE admission; hasLiveHoverLifecycle gate via
  the additive lc.type field keeps non-hover STOPs byte-identical;
  bounded settle poll, delivery mechanics not semantics.
  (c) Consuming-click ownership regression fix: in-scope completion
  release only when the lifecycle is the event's FIRST owner
  (!handled); state/endTime assignments restored.
  (d) T4 target-removed lifecycle wiring (reviewer round-1 FAIL
  remediated, round-2 PASS): completesOnTriggerRemoved definition flag
  (Hover-only), completeTriggerRemoved (terminal 'target-removed',
  completed, endTime=lastActivityTime, splice-before-complete),
  resolveTriggerRemovedLifecycle (lifecycleId exact + triggerEventId
  R-2 fallback), CS→SW TRIGGER_REMOVED notification fired at both
  collector producers (structural isConnected pass + handleTriggerRemoved),
  SW case → handleTriggerRemovedNotification (self-broadcasts
  INTERACTION_CAPTURED, handleObservedEvent parity).
  Deleted repealed-era suites; amended 5 legacy suites off deleted
  semantics. Verification: full suite 334 files / 4,982 tests green;
  tsc 8-baseline; real-Chrome zz-b7-p2-validate.mjs S1–S7 ALL PASS
  (S7 new fixture target-removed.html: completed, classes ["reveal",
  "removal"], no twin); B6 enter-submit PASS; B6.1 all three fixtures
  PASS; infra PASS (download-server mirror re-synced after T4 rebuild —
  RECURRENCE: the 8080 mirror copies at startup, restart it after every
  rebuild; public /download md5-matched); download README regenerated
  (install guidance + current checksums — stale 7.4-B5 text replaced);
  reviewer PASS round-2; tester 4/5 → README fix → clean. Not
  published/deployed (owner gate). NEXT: B7-P3 attribution/episodes/KR
  (zero KR schema change).
- 7.4-B7-P3 'Hover understanding — anchoring, surface-reuse provenance,
  KR persistence' (2026-08-28, SHIPPED — spec §5.3, acceptance record in
  spec §11): (a) ANCHORING: episode-builder gate switched to
  isAnchorEligibleInteraction (src/runtime/evidence-ledger.ts; ANCHOR_ELIGIBLE
  = DISCRETE_ACTION_TYPES membership for every non-Hover type; a Hover
  anchors iff endState completed ∧ metadata.consequenceClasses non-empty) —
  non-hover anchoring provably byte-identical (truth-table + horizon-parity
  pins); non-admitted hovers stay unowned, never members. (b) SURFACE JOIN:
  new pure module src/shared/surface-join.ts — deterministic joins between
  producer DOM-path facts (getElementPath grammar) and consumer identity
  grammar (cssSelector/xPath/stableId): id-exact (own-id spellings ONLY),
  chain-degraded (mid-chain id), tag-floor-degraded (≥2 contiguous
  suffix-aligned tag segments AND the fact carries ≥1 DOM id); no
  vocabulary/class heuristics anywhere. (c) PROVENANCE: causal-graph emits
  surface-reuse ProvenanceLinks (hover producer → click consumer) — strictly
  NON-CAUSAL (no CausalEdge, no registry claims, no confidence change);
  tab + recorded-order guards; deterministic prov-<producer>-<consumer> ids;
  FIRST-WINDOW FACT OWNERSHIP: a fact path is owned by the earliest
  reporting window, so INV-C1 global-accumulator replay (hover-into-open-
  panel re-reporting the earlier reveal) fabricates no links — found in
  real Chrome (int-2 re-reporting int-1's aria-flip/display:block with fMA≈1
  pre-dating its own open), fixed by the ownership map, pinned by 3 red
  tests. Pointer-reach admission join fixed to consume the shared join (the
  P2 exact-string-equality version was real-Chrome-dead — first real-Chrome
  confirmation it now fires, degraded-chain). (d) KR: zero schema change,
  mapper untouched; hover episodes persist as rows; signatures accumulate
  under the frozen appId:sig:hash(appId|actionType|normalizedTarget|
  anchorViewId) identity; consequenceProfile is edge-derived, so in
  reveal→consume the T4 winner (the click, latest overlapping anchor)
  legitimately owns the window consequence — verified: consuming Link
  signature carries T4-window|ui|post-anchor; a hover with NO later
  overlapping anchor wins T4 and carries its own profile entry (S3 mega-menu
  session); provenanceLinks counted in the session manifest coverage.
  (e) V3 pinned by two suites (hover-v3-supersession-pin + v3-settle-
  supersession-pin), not fixed (DC-4). Verification: suite 341 files /
  5,031 tests green; tsc exactly 8 pre-existing; real-Chrome
  zz-b7-p3-validate.mjs 7/7 PASS (S1 anchor+link+dom-ref+non-causal+
  signature-identity, S2 two-consumers 2 links from the owning producer,
  S3 gesture-only none, S4 unrelated-click no link), NEW
  zz-b7-p3-kr-validate.mjs 3 sessions PASS (schema verno 30 unchanged,
  episodes persisted, occurrenceCount 1→2 reinforcement, no key fork,
  hover-wins-T4 profile), B6 + B6.1 harnesses PASS; infra PASS (fixture
  servers 8094/8095 cleaned; vitest zombies noted); reviewer PASS (4
  cosmetic WARNs remediated); tester PASS 4/4 (download README regenerated
  to the P3 build + checksums, download-server restarted to re-sync — the
  8080 mirror recurrence again). Not published/deployed (owner gate).
  B7-P4 SHIPPED 2026-08-29 (entry above); doctrine note now at
  .drytis/notes/hover-doctrine-v1-v4-b7.md (V1–V4 + P4-amendment
  residuals + ambient-churn watch metric).
- 7.4-B6.1 'Form-less SPA Enter commit (family F2)' (2026-08-27, SHIPPED —
  spec `.drytis/specs/phase-7-4-b6-1-formless-spa-enter-commit.md`, full
  verification record in spec §12; closes the B6-flagged SPA gap): bare
  input + app's own Enter handler → fetch + pushState. Root cause
  (read-only F2 study + in-process replay): a navigation event flushes ALL
  active lifecycles interrupted (component-runtime process step 2), the
  interrupted TextEntry carries the typed value only in ctx.data, and
  filterProductionInteractions drops it — the same destruction shape as
  the B6 MPA bug, one layer up. Fix, two effect-grounded tiers (Enter
  keydown is NEVER a completion trigger — cause, not commit; doctrine
  preserved): TIER N (navigation, strongest effect) — new optional
  definition hook `shouldCompleteOnNavigation` consulted ONLY at the
  nav-flush seam; TextEntry implements P1 form-less (ownerFormOf null,
  B6/F1 isolation), P2 typed non-empty, P3+P4 terminal Enter via ONE
  canonical pure derivation `terminalEnterMemberOf`, P5 same-document
  navType guard (pushState/replaceState/popstate/hashchange — SW-minted
  full-page navs carry no navType); runtime two-pass hook consultation +
  winner election (latest trigger-scoped Enter captureSeq; ties → later
  lifecycle), losers interrupted with commit metadata scrubbed. Commit
  metadata: commitSignal='navigation', committedValue, enterCause,
  navType. IR order FILL→NAVIGATE is NATURAL (hook completes inside nav
  flush before Navigation discovery — zero reorder code, C-3 audit
  finding). TIER C (network+corroboration, STOP rescue) —
  `reconcileFormlessEnterCommits` at STOP between flush and projection:
  interrupted form-less Enter TextEntry rescued only on E-C1 exact
  network join (row.sourceEventId === enterEventId) AND E-C2 corroboration
  class from the interaction's OWN evidence (viewConfirmation |
  notificationSurfaceChange | fieldRemoval); commitSignal='network';
  FILL-only IR replay (no IR action can express bare Enter — honest
  incomplete, keyboard-press deferred). C-1 ledger immutability: rescue
  flips endState pre-projection, the EXISTING coveredEventIds path
  suppresses Unclassified twins, ledger dispositions never rewritten, zero
  projection-engine changes. REAL-CHROME FINDING (only the E2E caught it):
  Chrome fires a trusted `change` on the input BETWEEN the Enter keydown
  and the effect (implicit control commit, no form needed) — P4 gained a
  `change` exemption in the canonical derivation (change carries the same
  committed value; typing always emits keydown+input first); pinned by
  AC1e2 and reviewer FAIL-1 fixed by making tier C call the SAME
  derivation. Debounce-rider honesty: stamped fetch with no corroboration
  ⇒ NO commit (E2E S3). No-op Enter: NO commit, keydowns survive as
  Unclassified cards (capture guarantee, E2E S2). Side panel renders
  commit labels (form submit / Enter route change / Enter app request);
  blur completions render nothing. Verification: 18 new unit/integration
  tests (definitions 9, runtime 6+13 incl. T12 real-Chrome-shape tier-C
  regression + T13 production-filter pin, generation 4, sidepanel 4),
  real-Chrome E2E `.drytis/zz-b61-e2e.mjs` 3 scenarios self-asserting
  PASS, B6 suites + harness untouched PASS, full suite 318 files / 4926
  tests green, tsc 8-error baseline, reviewer PASS (2 rounds; FAIL-1
  tier-C canonical-derivation port + FAIL-2 docs fixed), infra_verifier
  PASS. NEXT: owner to pick — publish branch, 7.4-C→D capability layer,
  or housekeeping.
- 7.4-M1 'Affordance capture' (2026-08-25, SHIPPED — commit 1 `2cd31d1`
  src+tests+fixture, commit 2 spec+evidence+roadmap+handover; see
  `.drytis/specs/phase-7-4-m1-affordance-capture.md`): first slice of the
  owner's 7.4-A→E hierarchy toward human-like application understanding
  (A affordance facts → B control semantics → C behavioral meaning (already
  ~80% built) → D capabilities → E KR reinforcement). Root cause from the
  2026-08-24 RCA: modern apps (Tailwind/CSS-modules/bespoke utility classes)
  express interactivity in CSS, not semantics — and the engine DETECTED the
  affordance then DISCARDED it (resolveTarget Strategy 2 computed
  cursor:pointer/onclick only to pick the target). Fix = persistence + claim
  gate: DomContext.pointerCursor/clickHandler optional computed-fact booleans
  (extracted same synchronous capture window), Click gains 4th gate after
  isInteractiveElement → LP1 → W-B. Deterministic, zero site vocabulary,
  zero new browser surface. Identity/locators/executor untouched (replay
  must not depend on computed style). HONESTY preserved: non-pointer,
  non-instrumented, non-roled elements stay Unclassified (E2E V2/V6).
  Chrome-truth correction during E2E: cursor:pointer INHERITS to children,
  so an inner span under a pointer chip is itself affordance-carrying and
  claims Click on itself (no parent lift needed) — both truths unit-pinned
  (C4 explicit cursor:auto leaf → lift; C4b inherited leaf → claim on
  itself). Gates: 26 pins (suite 283/4,694), tsc 8, ZIP 889b8c4b four-way
  identical with tokens in shipped bytes, reviewer PASS (red-first
  reproduced 16-fail/9-pass), infra PASS (after serve-mirror fixture sync),
  E2E 16/0, house regressions 6E-M2 8/0 + 6F-M1 9/0. Environment truths
  documented in evidence PROVENANCE: execSync(curl) deadlocks inside ESM
  top-level-await (use node:http probe); http-server absent (harness ships
  its own static server); :8190 app-verbatim fixture must be started
  manually before house regressions. NEXT (superseded by 7.4-B1, shipped
  above): 7.4-B semantic control definitions — Expander shipped 2026-08-25.
- 7.3 W-B 'Generic QA auto-id recognition' (2026-08-24, SHIPPED — commit 1
  src+tests, commit 2 spec+evidence+roadmap; see
  `.drytis/specs/phase-7-3-wb-auto-id-generic.md`): the engine recognizes the
  bare `auto-id` attribute as a first-class industry QA test-ID family,
  closing the largest real-site classification gap from the 2026-08-24 user
  RCA ("most clicks unclassified" on results pages). Four layers: capture
  (`extractIdentity.autoId`, ''→null honest absence); resolution
  (`INTERACTIVE_SELECTOR` + `[auto-id]`/`[data-auto-id]` → ancestor lift,
  leaf-first so inner buttons/links win); classification (Click claims any
  QA-instrumented target via truthy autoId || dataAutoId — attribute-presence
  fact, no timing, isInteractiveElement untouched, S6/LP1 gate preserved);
  locators (`[auto-id="X"]` BUSINESS candidate; prefix-optional family regex
  in locator-resolver + executor-content-script mirror + Playwright renderer,
  decoy-isolated BOTH directions). elementKey dataAutoId omission FIXED
  (chain testId→dataCy→dataQa→stableId→dataAutoId→autoId→…; stableId stays
  ahead per 6B byte-identity rule). GENERICITY (owner constraint): auto-id
  is an industry convention, zero site tokens (doctrine pin green); fixes
  AdaniOne cards because AdaniOne uses the convention, not because it is
  AdaniOne. Gates: 29 W-B pins + 10 parity/renderer extensions (reviewer
  reproduced 17 fail / 12 pass red-first with src stashed), suite 280/4,669,
  tsc 8-baseline, ZIP md5 335442a0… four-way identical, real-Chrome E2E
  12 PASS / 0 FAIL (evidence/phase-7-3-wb-e2e-2026-08-24/) + house 6E-M2/6F-M1
  regression 9 PASS / 0 FAIL, reviewer PASS (WARN-1 codegen unit-pinned —
  documented), infra_verifier PASS. Honest limits: un-instrumented
  utility-class furniture stays honestly Unclassified; F4-L stays parked
  no-repro.
- 7.2-M1 'Close the knowledge loop' (2026-08-24, SHIPPED — commit 1
  src+tests, commit 2 spec+evidence+roadmap; see
  `.drytis/specs/phase-7-2-m1-knowledge-deeplink.md`): every knowledge
  signal in the panel is now a working door into the KR browser. KR chips
  are real buttons (Enter/Space-activatable) deep-linking
  `repository/index.html?app=&sig=` with the focused signature row
  highlighted (`.kr-highlight` + scrollIntoView, CSS.escape'd selector);
  the long-stale MS-U4 placeholder ("Knowledge browser arrives in MS-U4")
  replaced by an honest "Open in knowledge browser" link (only when
  opener+entityId+appId present — no fake affordance); header/browse
  repo buttons now open app-scoped via cached `resolveSessionAppId`;
  `parseKnowledgeParams` whitelist + unknown-app fallback to first app;
  `projectId` receive-side wired (closes the unscoped-heals audit gap).
  DECISION RECORDED: the roadmap's "KR browser in panel" is deliberately
  NOT built — the understanding card already holds the in-panel "what was
  learned" role; embedding the 11-section browser would duplicate it. The
  loop closes via panel → browser tab. Gates: 24 new/updated pins (reviewer
  independently reproduced 21/24 red-first), suite 277/4,630, tsc 8-baseline,
  real-Chrome E2E pin ⑫ 7 PASS / 0 FAIL (chip → tab → selected app +
  highlighted row, repo-btn scoped, zero console errors), reviewer PASS,
  infra_verifier PASS (ZIP 7057aa6b four-way identical, markers verified
  in the shipped artifact). REVIEWER WARNs recorded as FOLLOWUPS in
  `.drytis/notes/reviewer-warns-7-2-m1.md` (per owner instruction):
  cache-failure permanence in resolveSessionAppId, entity-branch E2E gap
  (unit-pinned), installKrLookup scoping change (accepted-not-reverted;
  fold remaining lookups onto the shared helper in a hygiene pass).
- WARN-4 (2026-08-24, SHIPPED — commit 1 src+tests, commit 2
  spec+evidence+roadmap; see
  `.drytis/specs/warn4-active-lifecycle-supersession.md`): the last 6F
  engine backlog item CLOSED. A discrete event absorbed by an ACTIVE
  lifecycle (step 3 handled=true) now supersedes that page's gesture
  records, mirroring step 2c's discipline. HONEST NARROWING: red-first
  evidence proved the pre-fix defect is exactly the **absorbed-CLICK**
  shape (absorbed keydown/mousedown sequences have their records
  superseded by arming mousedowns via 2c anyway; only the absorbed click
  leaves a record live AND intervenes in discrete order). The ordering
  guard `mousedownCaptureSeq < event.captureSeq` is load-bearing —
  without it the completing mousedown supersedes its own fresh record at
  birth (first fix attempt broke 4/8 6F-M1 pins; caught, reverted,
  guarded; reviewer independently verified guard necessity). Gates: WARN-4
  matrix 6/6 red-first (W4-T6 the unique pre-fix red), 6F-M1 matrix
  11/11, suite 4,606/4,606 (10 total runs; one non-recurring flake
  characterized in `suite-flake-observed-2026-08-24.md`), tsc 8-baseline,
  real-Chrome regression 9 PASS / 0 FAIL (house 6f-m1 harness on the
  AdaniOne clone), reviewer PASS, infra_verifier PASS, ZIP md5 8d1993af
  live-verified. Note: the stale phase-6e-m1 harness (pre-6E-M2
  expectations) honestly preserved — see spec §4b. 6F engine backlog now
  EMPTY.
- 7.0-KR App Identity Gate (2026-08-24, SHIPPED — commit 1 src+tests, commit 2
  spec+evidence+roadmap; see `.drytis/specs/phase-7-0-kr-app-identity.md`):
  sessions persisted under the **recorded app's origin** (not the panel URL,
  not per-path fragments). `normalizeWebOrigin`/`resolveRecordingOrigin`
  (`src/understanding/persistence/recording-origin.ts`, new); wiring at START
  stamp / M9.12 preload / stop origin; honest skip when no web origin
  resolvable. Real-Chrome proof (panel-first START, two sessions):
  ONE app row `http://127.0.0.1:<port>`, `sessionCount 2`, signature
  `occurrenceCount 2` (`Click/add to cart`, `Click/search flights`), shared
  entity, ZERO chrome-extension app rows. Evidence:
  `.drytis/notes/evidence/phase-7-0-kr-e2e-2026-08-24/`. This unblocks 7.1–7.4
  (cross-session reinforcement is now structurally possible). Existing
  panel-origin app rows left inert (read-only-KR doctrine; separate owner
  decision). 7.1-W1 SPA NAV → VIEW LAYER (2026-08-24, SHIPPED — commit 1
  src+tests+asset+manifest, commit 2 spec+evidence+roadmap; see
  `.drytis/specs/phase-7-1-w1-spa-nav-inject.md`): root cause — EventTap's
  history patch ran ISOLATED-world, so page-world SPA router calls were
  invisible (knowledgeViews/viewTransitions = 0 in EVERY dump ever
  captured). Fix = Option A: `public/assets/nav-inject.js` (MAIN-world,
  network-inject conventions) + `cmdrunner-nav` CustomEvent bridge in
  event-tap.ts + 4th manifest MAIN entry; downstream untouched. Real-Chrome
  proof: s1 views `["home","search-results"]`, transition
  `search-results->home`, one origin-only app row; s2 sessionCount 2,
  views shared, transition REINFORCED x2, signatures occ:2. BONUS:
  no-live-horizon gaps 1–4/session → 0. Evidence:
  `.drytis/notes/evidence/phase-7-1-w1-e2e-2026-08-24/`. 7.1-W2 FULL-RELOAD
  NAV → VIEW LAYER (2026-08-24, SHIPPED — commit 1 fixtures+test, commit 2
  spec+evidence+roadmap; see
  `.drytis/specs/phase-7-1-w2-full-reload-views.md`): grounding audit found
  the full-reload chain code-complete in src/ (onCommitted → synthetic
  full-reload evidence → Navigation interaction → extractor → view rows) —
  starved of PROOF, not code. Milestone = fixtures (public/search-form,
  search-results, cart.html — URLs deliberately match default view
  patterns via REAL form-GET document navigations) + repository-layer
  integration pins (incl. m9-unmatched-URL honesty pin) + real-Chrome
  E2E pin ⑪: 9 PASS / 0 FAIL — s1 views ["cart","search-results"],
  transitions search-results->cart AND cart->search-results, one
  origin-only app row, IR plan ["navigate","click",…×7] (4 NAVIGATE steps,
  no twin clicks); s2 views shared, transitions REINFORCED x2,
  sessionCount 2. ZERO src/ changes. ZIP md5 c30ca19f (bundle unchanged).
  Evidence: `.drytis/notes/evidence/phase-7-1-w2-e2e-2026-08-24/`.
  FULL-RELOAD NAV HARDENING CLOSED by 7.1-W2. PROVENANCE FIELD DECISION
  CLOSED: keep the shipped 4-value EntitySource vocabulary
  (target-derived|view-derived|content-observed|inferred) — more
  informative than the roadmap's older selector|changed-element wording;
  no code change (spec §2).
- 7.1 Ingestion quality: pin ⑨ + real-Chrome Dexie probe (seeded items land in
  cmdrunner_knowledge v30+); provenance field decision (origin:
  selector|changed-element); entity stability across sessions (6B
  signatures). STATUS: pin ⑨ + entity stability DONE via 7.0-KR; view
  ingestion DONE via 7.1-W1; full-reload nav DONE via 7.1-W2; provenance
  decision DONE via 7.1-W2 §2 (keep shipped EntitySource enum).
  **7.1 CLOSED 2026-08-24.**
- 7.2 Knowledge surfacing: "what the engine knows" inspection — entities,
  outcomes, episode patterns; KR browser in panel (Drytis explains what it
  LEARNED, not just observed).
- 7.3 Capability model creation (priority 3): capabilities DERIVED from KR
  (e.g. date-picker affordances, selection surfaces, counter behaviors per
  app) — emerging model, not a parallel rule engine; feeds 9.x consumers.
- 7.4 Cross-session learning proof: session 2 on same app shows improved
  recognition/derivation from KR alone — THE engine metric.
  SUCCESS METRICS: assertion coverage on real-site recordings; Unclassified %
  on benchmark flows; KR cross-session stability %; replay pass rate.

## PHASE 8 — Generation & execution durability (vision priority 4 enablers)
- 8.1 Replay durability (post-6B): stable locators across re-renders; D2/D3
  healing with BUSINESS tier; executor retry semantics.
- 8.2 Phase 5b API contracts: response-body capture → contract assertions +
  API test seeds v2 (revisit AFTER 6E — clone fidelity no longer misleading).
- 8.3 Codegen breadth: Playwright solid; IR as the interchange for additional
  targets (Cypress/Playwright-python/etc.) — AI-assistant-ready shape.
- 8.4 KL-1 rapid-cadence dialog cross-attachment: fix only if user cadence
  evidence shows it live (deferred since Aug-20).

## PHASE 9 — AI agents & assistants (the consumers)
- 9.1 AI test generation from KR (project-minted OpenAI-compatible key infra
  ready): "generate tests for checkout" reads entities/outcomes, never raw DOM.
- 9.2 Copilot explanations: session narratives from knowledge + learned
  context (📸 Resulting State → "what happened and why").
- 9.3 Agent-driven exploration: capability model guides autonomous browsing →
  grows KR → closes the learning loop (engine becomes self-improving).

## PARALLEL HOUSEKEEPING (any checkpoint)
- Publish to origin (mirror 8+ commits behind) — owner-gated.
- Version sync: manifest 10.9.0 vs package.json 10.4.18.
- Dead code: ~9.6k legacy lines, deterministic-recorder.ts.
- Harness /tmp persistence: harnesses auto-restore /tmp apps (bitten twice).

## SEQUENCING LOGIC
6-stabilize → 6B (identity feeds everything) → 6D.1+6E-slice (new observation
classes) → 6D.2/6E/6F → 7 (KR maturity on top of high-quality observation) →
8 (durability) → 9 (AI consumers). Phase 5b lands at 8.2. Every phase ends:
owner gate, matrix green, notes updated, doctrine intact.
