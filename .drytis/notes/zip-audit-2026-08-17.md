# Extension ZIP Audit — Read-Only Inventory (2026-08-17)

**Target:** `/workspace/cmdrunner-extension.zip` (md5 `a4d1a484fd1525d4bf52ac253b54770b`, 240,901 bytes, packed 2026-08-17 04:12, v10.9.0). The `download/` copy is byte-identical.
**Provenance:** SW bundle + manifest + recorder chunk are byte-identical (md5) to the pristine `d67588e` build validated end-to-end in real Chrome this session — the ZIP contains exactly the CP1–CP8-validated binaries.
**Extraction:** `/tmp/zip-audit/ext/` (read-only copy; original untouched). 26 files, 0 errors, 0 duplicates, no hidden/dot entries, no `_CRX` junk entries, all paths relative.

## Verdict: READY FOR MANUAL INSTALLATION

Valid MV3 package: manifest at root, all manifest-referenced files present
(loader, content scripts, pages, icons), all HTML→chunk references resolve,
all 18 asset chunks reachable via import graph, no source maps, no `.drytis`,
no `node_modules`, no test/validation HTML (packer excludes root-level HTML),
no logs/screenshots/temp files, ZIP CRC clean.

Minor findings (informational, none block installation):
- `assets/network-inject.js` (unhashed, 7,227 B raw) is referenced ONLY by
  `chrome.scripting.executeScript({files:["assets/network-inject.js"]})` inside
  the SW — it IS runtime-required (MAIN-world injection fallback). The hashed
  `network-inject.js-4DBfkNVo.js` is the manifest content script (same logic,
  minified). Both are needed; they are not duplicates.
- CP8 `KnowledgeContract` is NOT in any shipped chunk — expected and correct:
  no production consumer imports it yet (CP8 is a library surface; its
  real-DB exercise in the E2E run used a probe bundle). CP1–CP7 runtime is
  fully present in `service-worker-inline.js` (cmdrunner_knowledge, tier
  markers, merge/mapper markers all found).
- `manifest.json` version `10.9.0` vs `package.json` `10.4.18` — manifest is
  the authoritative extension version; noted, not a defect.
- Benign strings only: "localhost" appears in AI-provider example URLs
  (Ollama default base URL, overridable); "password"/"Bearer"/"apiKey" are
  input-type checks, login-field heuristics, and AI-provider auth header
  builders reading `chrome.storage` — no embedded credentials or secrets.
- One "TODO" match inside the SW bundle is a minified identifier fragment
  (not a code comment).

## Size profile
- Raw 907,656 B → ZIP 237,399 B (ratio 0.262).
- SW 570 KB raw / 142 KB zipped (62.8% of package) — includes the entire
  understanding pipeline, knowledge persistence (Dexie), recording stack.
- Nothing removable: every file is runtime-reachable. Estimated savings from
  removals: **0 bytes** — the packer already excludes dev/test HTML, `.ts`
  sources, and dot artifacts.
