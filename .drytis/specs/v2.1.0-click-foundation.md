# Task: v2.1.0 — Click Interaction Foundation (Phase 1)

**Baseline:** v2.0.0-alpha (commit 4c6b0b8)

## Scope
`click-content-script.ts` ONLY. No other files.

## Phases

### 1A — Target Resolution
- [ ] `resolveClickTarget(rawTarget, event)` using `event.composedPath()` for shadow DOM
- [ ] `INTERACTIVE_SELECTOR` constant covering native tags + ARIA roles + interactivity signals
- [ ] SVG/span/img/path inside button → resolves to button
- [ ] Element reference stored in PendingClick replaced with resolved identity at capture time
- [ ] DOM detachment safe (extract identity before 300ms timeout, not at emit time)

### 1B — Ownership Foundation
- [ ] Check `data-cmdrunner-handled` attribute on resolved target + ancestors
- [ ] If present → skip click emission
- [ ] Placed AFTER target resolution, BEFORE double-click dedup

### 1C — Double-Click Dedup
- [ ] Replace `elementsMatch(a, b)` (ancestor/descendant) with exact normalized comparison
- [ ] Two different children of same parent clicked in succession → both recorded

### 1D — Synthetic Click Protection
- [ ] `if (!event.isTrusted) return;` at top of click handler
- [ ] Programmatic `.click()` calls ignored

### 1E — Accessible Name Improvements
- [ ] Multi-ID `aria-labelledby` (space-separated → resolve each ID, join)
- [ ] `<label for="id">` association for form controls
- [ ] `<img alt="">` fallback for images
- [ ] `<input type="image" alt="">` fallback

## Acceptance Criteria
- [ ] Build succeeds
- [ ] All existing tests pass (437)
- [ ] New comprehensive click tests pass
- [ ] No regressions in other interactions
- [ ] Changes isolated to click-content-script.ts

## Out of Scope
Locator strategy, stable ID filtering, step-builder, service-worker, AI, pipeline, enterprise interactions.
