node_modules/
dist/
*.zip
.DS_Store
*.log
.env

# Test/debug screenshots (not part of the extension)
.playwright-mcp/
repository-page.png
repository-tree-expanded.png
settings-about.png
sidepanel.png
*.tar.gz

# Temporary/debug artifacts
tmp/
*.py
download/

# Build snapshot directories (not source — contain node_modules/dist/zips)
baseline-*/

# Runtime download-server root (background service 3657 — contains only the ZIP + README copy)
serve-download-root/
serve-download/
